# ios-feed

## Telegram Channel

The bot posts to the public channel `@ios_daily`.

To read post content by ID, fetch `https://t.me/s/ios_daily/<id>` via WebFetch — the `/s/` path returns static HTML with the full message text, links, and hashtags.
